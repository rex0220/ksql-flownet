# 【kSQL-FlowNet #4】運用編 下書きの外部レビュー裁定(2026-09-07)

対象: docs/internal/qiita-draft-4.md(commit 8e8dfd4)。ChatGPT の指摘を仕様書 §6.4〜§6.7・§7.3 と突き合わせて採否を決めた。

| # | 指摘 | 裁定 | 反映・根拠 |
| --- | --- | --- | --- |
| 必須 1 | START 許可 CSV は認可ではない。「一覧にない network は NETWORK_NOT_ALLOWED」は誤り | 採用 | CSV は入力候補、自由入力でも三重ゲートを満たせば起動できる、拒否されるのは allowlist にない/app_start 無効の network、と書き換え(仕様書 §6.4・§7.4) |
| 必須 2 | 補正モードで対象期間と業務キーの両方を渡すと KEY_POLICY_MISMATCH になるのでは | 不採用(表は正しい) | 仕様書 §6.5: scheduled_period の補正は business_key と scheduled_for の **両方必須**(片方だけなら AS_OF_UNDEFINED)。KEY_POLICY_MISMATCH は explicit に scheduled_for を付けた場合。表に「両方必須」「対象期間は as_of に使われる」「片方だけだと AS_OF_UNDEFINED」を追記 |
| 追加 1 | IDLE の行がボタン表にない | 採用 | 「ボタンなし。Run レコードはあるが未開始(started_at なし)。起動処理が続けば LIVE、長く IDLE なら二次対応者へ」を追加(仕様書 §7.3) |
| 追加 2 | フロー図の CLI 名を抽象化(RELEASE が見えない) | 採用 | 「要求に対応する CLI 操作を実行」へ |
| 追加 3 | RETRY_BRAKE の表現 | 採用 | 「同じ失敗が 3 回続いたため、次の定期 resume でも再実行しない安全装置が働いている」へ |

## 第 2 巡(e3d4a6f に対して)

| # | 指摘 | 裁定 | 反映・根拠 |
| --- | --- | --- | --- |
| 確認 | 補正モードの「両方必須」が P2-11 の確定仕様(排他)と異なるのでは | 不採用(記事が正) | P2-11 仕様は第 4 巡 N-1 で「correction = business_key + scheduled_for 両方必須、business_key 単独は AS_OF_UNDEFINED」へ改訂済み(§3 表)。実装 `src/requests/start-request.ts`、単体 `poll-requests-start.test.mjs`、実機 E2E `p2-11-04-rejections` で確認。本番でも補正 START(両方指定)が実行済み(R0 記録)。提案の 1 文(定期キーを導出せず指定キーを使う、基準時刻のため対象期間も必須)と実機確認済みの旨を追記 |
| 微修正 1 | RERUN は「失敗ノードから」に限らない(INTERRUPTED は未完了位置から) | 採用 | 「未完了の位置から再開。FAILED なら失敗ノードが再実行対象」へ |
| 微修正 2 | 「出ているボタンがいま許される操作」は強すぎる | 採用 | 「画面の判定時点で要求できる操作。状態が変わっていればポーラー側で安全に拒否される」へ |

## 第 3 巡(0fba2dc に対して)

| # | 指摘 | 裁定 | 反映 |
| --- | --- | --- | --- |
| 1 | START モードの表の途中に段落が入り、任意キー行が表として崩れる | 採用 | 段落を表の後ろへ移動 |
| 2 | 「片方だけだと AS_OF_UNDEFINED」は不正確(対象期間だけなら定期キー) | 採用 | 「両方必須。業務キーだけは AS_OF_UNDEFINED。対象期間だけは補正ではなく定期キー扱い」へ |
| 3 | 「既存レコードを編集しない」が取消(cancel_requested 更新)と矛盾して見える | 採用 | 「内容や状態フィールドを直接編集して再依頼しない。取消だけはボードの取消ボタンから」へ |
| 4 | まとめの「いま許される操作」を本文と統一 | 採用 | 「画面の判定時点で要求できる操作」へ |
