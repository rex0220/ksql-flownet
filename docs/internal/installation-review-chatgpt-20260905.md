# 導入手順書(docs/installation.md)ChatGPT レビューの裁定(2026-09-05)

対象: docs/installation.md(commit 04e1d68)。レビュー観点は新規導入。指摘ごとに正本(統合仕様書・P2-16 仕様・実装・本番構成)と突き合わせて採否を決めた。

| # | 指摘 | 裁定 | 根拠・反映 |
| --- | --- | --- | --- |
| 1 | 一次対応者の権限「閲覧・追加」では取消(既存レコードの PUT)ができない | **採用** | kintone のレコード更新にはアプリのレコード編集権限が必要。P2-16 仕様 U-4 と §3.4 はフィールドアクセス権(作成者=編集可)だけを定めており、アプリ権限を書いていなかった。本番 smoke は管理者アカウントで行ったため未検出。§4 の表を「閲覧・追加・編集」に変更し理由を明記、「既存レコードの編集はせず」を「取消フラグの更新を除き」に修正。templates/README・plugin/README・仕様書 §8.1 も同じ内容に揃えた。一次対応者相当アカウントでの取消 smoke を R5 の確認項目に追加 |
| 2 | §5 の START 許可 CSV 例(`monthly_deal_summary`)が §8 以降(`monthly_summary`)と不一致 | **採用** | 本番の network_id をそのまま書いた誤り。`月次集計(当月分の起動), monthly_summary, 定期` に統一し「手順 8 の例と同じ network」と注記 |
| 3 | 取消 smoke の成立条件(取消が効いたことの再確認、他の未処理要求がないこと、検証範囲の明記)が不足 | **採用** | ポーラー実行前に `REQUESTED` かつ `cancel_requested=取消` を再読込で確認し、確認できなければ実行しない手順を追加。事前に `01_未処理要求` が空であることを確認する手順を追加。smoke の範囲(起票・取消・書戻しまで。allowlist 受理と子起動は含まない)を明記 |
| 4 | API トークン発行後の「アプリを更新」が抜けている | **採用** | kintone のトークンはアプリ更新まで有効にならない。§3 に 1 文追加 |
| 5 | 「受信ポート不要」が SSH 前提と矛盾 | **採用** | 「kSQL-FlowNet の稼働用の待受ポート・固定 IP・ドメインは不要。管理用の SSH 接続経路は別途必要」に修正 |
| 補-1 | SQL の実配置パスを明示 | 採用 | §6 のツリーに flow フォルダー配下の実パスを追加、§8.2 の `sql:` 行に絶対パスをコメント |
| 補-2 | `chmod +x` を掲載 | 採用 | §8.4 に追加 |
| 補-3 | root の `crontab -e` を明記 | 採用 | §10 に追加 |
| 補-4 | cron の PATH と `node` の探索 | 採用 | §10 に「`/usr/bin` 以外なら `which node` の絶対パスを使う」を追加。本番 VPS は `/usr/bin/node` のため `node` で動作している |
| 補-5 | 業務アプリのトークン発行・dry-run へのリンク | 採用 | §1 のジョブ資材の行から kSQL-Flow README のクイックスタートへリンク |

未採用はなし。指摘 1 は文書だけでなく権限方針の欠落だったため、正本(仕様書 §8.1)へも反映した。
