# R3 アプリテンプレート試験インポート記録(2026-09-05)

release-plan R3。配布テンプレート `templates/ksql-flownet-apps-1.0.0.zip`(kintone 登録名「kSQL-FlowNet 1.0.0」)を、本番と同じドメインの検証用スペースへインポートし、テンプレートに含めた内容と参照の張り替え・プラグインの自動検出を確認した。実施はユーザー(kintone 操作)、確認項目の設計と記録は Claude。

## 作成されたアプリ(検証スペース)

| アプリ | ID |
| --- | --- |
| kSQL-FlowNet 実行管理 | 4279 |
| kSQL-FlowNet 操作要求 | 4278 |
| kSQL-FlowNet 監査履歴 | 4280 |
| kSQL Flow 実行ログ | 4281 |

## 確認結果(すべて OK)

| # | 確認項目 | 結果 |
| --- | --- | --- |
| 1 | 実行管理の関連レコード 3 種(`related_audit_events`・`related_requests`・`related_job_logs`)の参照先が新アプリ(4280 / 4278 / 4281)を指す。本番アプリ(4262 / 4268 / 4249)を指していない | OK |
| 2 | 実行管理の設定 → プラグインに「kSQL-FlowNet Run状況」が入っている(テンプレートのプラグイン参照が有効) | OK |
| 3 | プラグイン設定を保存しない状態で「00_Run状況」を開くと空のボードと「新規実行」ボタンが表示され、Console に `plugin v1 loaded` が出る(関連レコードからのアプリ ID 自動検出=ゼロコンフィグ) | OK |
| 4 | 操作要求に CLOSE / CANCELLED / 「取消」チェックボックス / 一覧 `01_未処理要求`・`02_拒否された要求`・`03_取消済み` がある(P2-16 込みの最終 schema) | OK |
| 5 | 4 アプリにアイコンとアプリの説明が入っている | OK |

## 割愛した確認

- プラグイン未読込のドメインでインポートした場合の挙動。本ドメインには本番用プラグインが読込済みで、外すと本番に影響するため試せない。導入手順書は「先にプラグインを読み込む」順序で固定している

## 次

- 検証スペースはこのまま R5(クリーンインストール検証)に使う。API トークン・フィールドアクセス権・プラグイン設定値はテンプレートに含まれないため、R5 で導入手順書どおりに設定する
- R5 では一次対応者相当のユーザー(操作要求アプリ: 閲覧・追加・編集、管理権限なし)を用意し、取消 smoke の権限を実機確認する(ChatGPT 導入手順書レビュー指摘 1)
