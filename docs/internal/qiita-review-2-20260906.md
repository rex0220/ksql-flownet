# 【kSQL-FlowNet #2】導入編 下書きの外部レビュー裁定(2026-09-06)

対象: docs/internal/qiita-draft-2.md(commit f95da02)。ChatGPT の指摘を導入手順書・実機構成・公式情報と突き合わせて採否を決めた。

| # | 指摘 | 裁定 | 反映・根拠 |
| --- | --- | --- | --- |
| 必須 1 | CLI のバージョンを固定(`@1.0.0`)、ジョブ資材は `npm ci` | 採用 | `npm install --global @rex0220/ksql-flownet@1.0.0` と `which` による絶対パス確認、ジョブ資材は `npm ci`(package-lock.json あり)。前提に「CLI・プラグイン・テンプレートを v1.0.0 でそろえる」を追加 |
| 必須 2 | NodeSource の `setup_22.x` スクリプトは非推奨。curl 未導入で失敗する | 採用 | 署名鍵を `/etc/apt/keyrings` に置く apt リポジトリ方式へ差し替え、`ca-certificates curl gnupg` を先に導入。本番 VPS も deb822 形式の NodeSource リポジトリで導入されていることを確認。Claude Code の指示文も同じ方式に更新 |
| 必須 3 | 一次対応者からアプリ管理を外し、導入担当と役割を分ける | 部分採用 | 導入手順書 §4 は 2026-09-05 のユーザー決定で「一次対応者=アプリ管理者」。記事では「導入担当のアプリ管理者に 3 アプリのアプリ管理、一次対応者は閲覧(+操作要求の追加・編集)」を基本として書き、筆者の運用では兼ねている旨と分ける場合の扱いを併記。手順書は変更しない |
| 必須 4 | root 運用を本番推奨にしない。タイトルの「本番運用まで」 | 採用 | サーバー側の冒頭に「検証用 VPS へ root で導入した例。本番では管理ユーザーと実行専用ユーザーを分け、root の定期実行を避ける」を明記。タイトルを「0 から運用開始まで」へ変更。実行ユーザー分離の配置例は記事に載せず、導入手順書の改訂候補(v1.1)として backlog へ |
| 推奨 1 | cron から `$(which …)` を外し、`PATH` 明示と絶対パス、`flock -n` | 採用 | crontab 先頭に `PATH=`、CLI は `/usr/bin/ksql-flownet`、両行に `flock -n`。FlowNet 自体のロックとの関係も 1 文で説明。ポーラー用ラッパースクリプトは記事では採らず(手順書と同じ 1 行構成を維持) |
| 推奨 2 | logrotate に触れる | 採用 | cron 節に 1 文追加 |
| 推奨 3 | セキュリティ更新と Node.js の LTS 更新方針 | 採用 | 初期設定の直後に 1 文追加 |
| 推奨 4 | Claude Code の「実行前に表示し承認」は権限設定次第。SSH を許可した AI は秘密ファイルを読める | 採用 | 「権限設定を承認必須にして作業」「秘密ファイルを読むコマンドは承認しない運用」へ書き換え |
| 軽微 1 | 「サーバー側 5 手順」は準備を含めると 6 段階 | 採用 | 「準備+5 手順」へ |
| 軽微 2 | 既存 kintone 契約の前提を費用の文に残す | 採用 | 「既に kintone を契約している前提」を追記 |
| 軽微 3 | 価格ページへのリンク | 採用 | 費用の行にリンク |
| 軽微 4 | smoke の見出しを強める | 採用 | 「#### ジョブを動かさず、操作経路だけを smoke する」を追加 |

## 導入手順書への持ち帰り(v1.1 候補)

- 実行専用ユーザーと管理ユーザーの分離(環境ファイルの所有と 0640、`/etc/ksql-flownet/`、`/var/lib/ksql-flownet/`)
- cron の `PATH` 明示と `flock -n`、logrotate の設定例
- NodeSource の鍵付きリポジトリ方式(手順書は Node.js の導入方法を規定していないため、参考として)
