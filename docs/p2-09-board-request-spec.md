# P2-09: ボードからの操作要求起票 仕様書

- 文書状態: **REVIEWED**(Phase 2作業単位。凍結仕様の変更なし — P2-08プラグインの拡張と、P2-08仕様・一次対応文書の受入/方針改訂を伴う)
- 起案日: 2026-09-01 / 改訂: 同日外部レビュー第1巡7+3点、第2巡5点、第3巡6点、M0 Codexレビュー17所見をすべて採用(§8に判断記録。第2巡指摘1はP2-10へ切り出し)
- **[実装計画](./p2-09-implementation-plan.md)§2のM0補正を実装時の補足契約とする**(POST body契約と作成後parse確認、終端GETのquery/安定順/総件数/セクション別失敗分離、pending GETのchunk・複数件表示(最古#id+件数)、**全30セルのstatus×activity判定表(異常組合せはINVALID=判定不能でfail-closed)**、表示優先順位(判定不能>処理待ち>RERUN無効>matrix)、G-09改訂の4層検証(型+call記録+bundle検査+実機network)、受入の未カバー7群の単体/実機への落とし込み)
- 正本参照: [p2-08-activity-plugin-spec.md](./p2-08-activity-plugin-spec.md)(拡張元)、[p2-01-app-rerun-spec.md](./p2-01-app-rerun-spec.md)(要求モデル・受理範囲の正本 — 本仕様は**入口を増やすだけ**で受理側は無変更)、[討論記録](./kintone-ops-roadmap-discussion.md) §13.2(機械専用制約)

## 1. 目的と非目的

**目的**: (a) Run状況ボード(P2-08)から、リラン・停止・解除の**操作要求レコードを起票**できるようにする。(b) ボードへ**「要対応(終端)」セクションを追加**し、一次対応者の「やることリスト」を1画面に統一する(現状、最頻の復旧対象である夜間FAILEDがボード外にある分裂の解消 — レビュー指摘2)。

**但し書き(第2巡指摘1)**: リランせずに決着した終端Run(業務側で手作業対応した等)を**リストから閉じる手段は現行製品に存在しない**(`lifecycle_status = ARCHIVED`を書く経路が実装にない — コード確認済み)。閉じる手段は**[P2-10](./implementation-plan.md)(CLOSE要求→`archive-run` CLI→ポーラー経由でARCHIVED化)**として別起票し、本仕様の抽出条件`lifecycle_status = ACTIVE`はP2-10完了時に初めて実効化する。**暫定の時限フィルタ(最終更新N日以内)は採用しない** — 未対応のFAILEDが黙って消える方が、決着済みが残ることより危険なため。それまで決着済みRunは上限20件+`updated_at desc`の下に自然に沈む運用とする。

**原則**: ボタンは**操作要求アプリへレコードを1件作るだけ**。実行は稼働中のポーラー(P2-01)経由の非同期であり、プラグインは判断・実行をしない。書込先は操作要求アプリのみ — **実行管理・監査アプリへは今後もGET限定**(機械専用制約は不変)。

**非目的**: resolve-node・force-unlock・`--approved-by`など判断を伴う復旧操作のアプリ化(CLIのまま)。即時実行・ポーラー周期の変更。受理範囲の変更。モバイル。

## 2. ボードの構成変更: 「要対応(終端)」セクション

ボードを2ブロック構成にする:

1. **未終端Run**(既存): activityバッジ4値。§7.4の凍結契約どおりactivityは未終端のみ
2. **要対応(終端)**(新設): `status in ("FAILED","CANCELLED","UNKNOWN")`かつ`lifecycle_status = ACTIVE`のRunを、**activityバッジなしの別ブロック**として表示(凍結契約に抵触しない)。`updated_at desc`で上限20件+**「他N件(決着済みを含む)」**表示(第3巡指摘5 — P2-10完了までNは未対応件数を意味しないため、誤読させるラベルにしない)。列はレコード番号リンク/Business Key/Run ID/Status/更新時刻/操作
3. **リランボタンの表示条件はポーラーの受理条件と同じ2条件に揃える(第2巡指摘2)**: `resume_allowed = true` **かつ** `lifecycle_status = ACTIVE`。現行実装では両値とも常に真だが、片方でも書けるようになった瞬間(P2-10のARCHIVED、P2-03残余のresume_allowed=false化)に「押しても必ず拒否されるボタン」が出る欠陥を先回りで防ぐ。**2条件で弾かれる行は操作列を空にせず「再開が無効化されています」を表示**(第3巡指摘3 — ボタンが出ない行には理由を書く、で全行統一)
4. **処理待ちバッジ(第3巡指摘1 — 重複ガードの完成形)**: 要求アプリ設定済みの場合、ボード描画時に表示中Runの`run_id`集合で`REQUESTED`/`ACCEPTED`要求を`in (...)`で一括GET(P2-08のInvocation照合と同じchunk化)し、**処理待ちの行は操作ボタンの代わりに「要求処理待ち #\<record_id\>」(要求レコードへのリンク)を表示**する。押す前に状態が見えるため、5分の待ち時間中の再押下がそもそも発生しない。このGETが失敗した場合はfail-open(バッジなし・ボタン表示 — 最後の重複ガードが引き続き効く)

## 3. 操作メニュー(状態連動の出し分け)

| 表示場所・状態 | ボタン | 起票内容 | ダイアログの必須注意文言 |
| --- | --- | --- | --- |
| 未終端: `INTERRUPTED`(赤) | リラン要求 | `RERUN` + run_id | 「中断分の結果はリラン時に裁定されます(確定できない場合はUNKNOWNとして保護)。**要求のresult_codeがOK以外、または再び中断する場合は二次対応者へ連絡**」 |
| 未終端: `LIVE`(緑) | 停止要求 | `STOP` + run_id | 「停止は次のノード境界まで効きません(実行中SQLは完走します)」 |
| 未終端: `STOPPED`(黄) | 解除要求 | `RELEASE` + run_id | ダイアログに**停止要求の要求者と理由**(CANCEL_REQUESTのJSONパックから)を表示し、「本人に確認しましたか?」の確認+「解除後、次の定期resumeが再開し得ます」(指摘5) |
| 未終端: `IDLE`(灰) | (ボタンなし) | — | 操作不要(定期起動を待つ) |
| 要対応(終端): `FAILED` / `CANCELLED` | リラン要求 | `RERUN` + run_id | — |
| 要対応(終端): `UNKNOWN` | **(ボタンなし)** | — | 「二次対応者へ連絡」の固定文言+**Run IDのワンクリックコピーボタン**(第3巡指摘4 — 「Run IDを添えて」を実際に守れる導線。機能しないリランボタンで誤誘導しない) |
| レコード詳細画面 | 上記と同じ出し分け(status/activityから決定) | 終端`FAILED`/`CANCELLED`のみ任意`rerun_from_node`欄あり | `rerun_from_node`は「二次対応者からRETRY_BRAKE解除等で具体的なNode IDを指示された場合のみ入力」の上級欄。**終端`SUCCESS`はボタンなし**(表示は従来の「終端(activityなし)」のまま) |

共通仕様:

1. **理由必須**の入力ダイアログ→操作内容の確認→POST。成功時「要求 #\<record_id\> を作成しました。**最大5分ほどで処理を開始します。完了は要求レコードの状態(DONE/REJECTED)でご確認ください**」+要求レコードへのリンク(指摘7 — 開始と完了を区別)
2. **重複要求ガード(指摘4)**: 起票前に同一`run_id`で`REQUESTED`/`ACCEPTED`の要求が既にないかGET確認し、あれば起票せず**既存要求へのリンク**を表示(「処理待ちの要求が既にあります」)。再読込を挟んだ再押下にも効く。これは審査ではなく親切(すり抜けてもポーラー側が正)。**ガードGETが失敗した場合(閲覧権限なし・通信断)はfail-openで起票を許可**し、「重複確認ができませんでした(処理待ち要求が既にある可能性があります)」をダイアログに表示する(第2巡指摘3 — 哲学どおりポーラーが正)
3. 二重送信防止(送信中はボタン無効化)。POST失敗はエラー明示のみ(自動リトライしない)。**POSTの403は専用文言**「操作要求アプリへの追加権限がありません。管理者へ連絡してください」(指摘6)。ガードGETの403はこの文言に流さず、前項のfail-open文言側で扱う
4. **受理判定をプラグインへ持たせない**: 出し分けは無駄な要求を減らす親切であって審査ではない。正の受理・拒否はポーラー+CLIの既存規則(P2-01 §4)が行い、拒否理由は要求レコード(02_拒否一覧)に残る
5. **要求者の真正性は不変**: レコード作成者=kintoneログインユーザー(プラグインは代筆しない)。起票フィールドは`request_type`/`run_id`/`reason`(+詳細画面のみ`rerun_from_node`)の人間欄だけ

## 4. 実装方式

- プラグイン設定へ**操作要求アプリID**を追加(2項目目。`auditAppId`と同じ検証・保存方式)。未設定の場合はボタン・要求GETを行わず、**起票機能に関して挙動不変**。ただし**要対応(終端)セクションは設定に関係なく表示される**(読み取りのみの拡張)ため、プラグイン更新だけで本番ボードの見た目は変わる — **M4の本番適用前に一次対応者へ表示変更を周知する**(第2巡指摘4 — v2の「設定するまで挙動不変」の記述は起票機能に限定するよう訂正)
- POSTは操作要求アプリの`/k/v1/record.json`(単票)のみ。実行者権限(APIトークン不使用) — 一次対応者に必要な権限: 操作要求アプリの**追加**(+重複ガードのための**閲覧**)
- ダイアログはプラグイン自前DOM(kintone内部DOM非依存)。表示はtextContent系のみ(P2-08と同じXSS規律)
- 起票後はボードを自動で1回再読込

## 5. 既存文書の改訂点(本仕様の採用と同時に反映 — 指摘1)

**一次対応方針の明示的変更**: `INTERRUPTED`の一次対応を「二次対応者へ連絡」から**「まずボードからリラン要求を1回。要求のresult_codeがOK以外・再中断・繰り返す場合は二次対応者へ連絡」**へ変更する。根拠: 中断Runのresumeはensure-runの裁定で保護され(結果確定できない場合はUNKNOWNで停止)、安全側に倒れる。ただし**常に完走するとは限らない**(UNKNOWN裁定時はresolve-nodeが必要 — m6実機で確認済みの挙動)ため、エスカレーション条件を文言に含める。

- [p2-08-activity-plugin-spec.md](./p2-08-activity-plugin-spec.md) §2.1の`INTERRUPTED`一次対応列 → 上記へ改訂
- [ops-first-response.md](./ops-first-response.md) の`INTERRUPTED`行と操作手順 → 同上+「ボードから起票(推奨)/操作要求アプリ直接(従来)」の2経路化。あわせて**STOPしたRunの表示移動を1行説明**: 「止めた直後は上のブロック(黄)、ノード境界で完全に止まると下の要対応ブロック(CANCELLED)へ移る」(第2巡指摘5)
- P2-08 §4「書込みゼロ(G-09)」→「**実行管理・監査アプリへはGET限定。書込は操作要求アプリへのレコードPOSTのみ**(cursor・Bulk・PUT/DELETEは引き続き禁止)」、受入4を同範囲で再定義

## 6. 受入基準(実機E2E)

1. ボードのINTERRUPTED行からリラン要求→要求レコード(作成者=操作ユーザー)→ポーラー受理→resume完走→`DONE`(一気通貫)
2. LIVE行から停止→次ノード境界で停止→`DONE`。STOPPED行から解除(ダイアログに停止要求者・理由が表示される)→hold解除→`DONE`
3. **要対応(終端)セクション**にFAILED Runが表示され、そこからリラン要求→resume完走。UNKNOWN Runは表示されるがボタンがなく連絡導線のみ
4. 詳細画面: 終端FAILEDからのリラン(+`rerun_from_node`が`--rerun-from`として効く)。終端SUCCESS/UNKNOWNにボタンが出ない
5. **出し分けの直接検証**: STOPPED行にリランボタンが出ない、LIVE行に解除が出ない等、§3の表どおりであること
6. 拒否系: 出し分けをすり抜けた要求(表示直後の状態変化等)がポーラーで`REJECTED`+理由になり、プラグインは壊れない
7. **重複ガード**: 処理待ち要求があるRunで起票を試みると既存要求リンクが出て2件目ができない。**ページ再読込を挟んだ再押下**でも同様
8. **403分岐**: 追加権限なしユーザーで専用文言が表示される(汎用エラーにしない)
9. ネットワーク実機確認: 閲覧はGETのみ、起票は操作要求アプリへのPOST 1件のみ。実行管理・監査へのPOST/PUT/DELETEがない
10. 要求アプリID未設定時はボタン・要求GETがなく、要対応(終端)セクション以外は既存P2-08と完全一致
11. 二重クリックで要求2件不可、reason空は送信不可
12. 文書整合: §5の3文書の改訂が本仕様の文言と一致
13. **(単体のみで担保 — 実機再現不能)** view model単体で、`resume_allowed = false`および`lifecycle_status = "ARCHIVED"`のRunにリランボタンが出ず「再開が無効化されています」が表示されること。**現行実装では両値を書く経路が存在しないため実機検証できない — P2-10でフィルタが実効化する前に単体で担保しておく**(第3巡指摘2a)
14. ガードGET失敗時のfail-open: 閲覧権限なし/通信断でガードGETが失敗しても起票は成功し、「重複確認ができませんでした」注記が出る(**POST 403の文言にならない**)(第3巡指摘2b)
15. 処理待ちバッジ: 処理待ち要求のある行にボタンの代わりに「要求処理待ち #\<id\>」リンクが出る。バッジGET失敗時はfail-openでボタン表示に戻る
16. UNKNOWN行のRun IDコピーボタンが動作する

## 7. 作業分割(想定)

| # | 作業 | 内容 |
| --- | --- | --- |
| M0 | 仕様確定 | 本仕様**最新版**のレビュー(Codex)→§5の既存文書改訂を同時反映。**チェック項目: 仕様へ条件を追加したら受入§6へ同時追加されているか**(第3巡指摘2の教訓) |
| M1 | 起票基盤+終端セクション | 設定2項目目・要求POST/重複ガードGETクライアント・出し分けview model・要対応(終端)ローダ(単体: matrix・POST body・重複・403・XSS) |
| M2 | UI結線 | 2セクションボード・行ボタン・詳細画面ボタン・ダイアログ(停止理由表示含む)(単体: DOM非増殖・失敗表示) |
| M3 | 実機受入 | スパイク環境で受入1〜11、証跡 |
| M4 | 文書・本番適用 | §5改訂・plugin README・**一次対応者への表示変更周知**・本番プラグイン設定追加 |

規模: **M**(要対応セクション追加によりv1のS〜Mから拡大)。実装はCodex、レビュー・実機受入はClaude Code。

## 8. レビュー対応記録(2026-09-01 外部レビュー)

| # | 指摘 | 採否・判断 |
| --- | --- | --- |
| 1 | INTERRUPTED方針がP2-08と矛盾 | **採用** — 方針変更を明示(§5)。「まずリラン→ダメなら連絡」へ。UNKNOWN裁定で完走しないケースがあるためエスカレーション条件を文言化 |
| 2 | 要対応が2画面に分裂 | **採用・P2-09に内包**(P2-10へ分割しない) — 動線一元化が本仕様の目的そのもの。要対応(終端)セクション新設(§2)。凍結契約(activityは未終端のみ)には非接触 |
| 3 | 終端SUCCESS/UNKNOWN詳細が未定義 | **採用** — SUCCESS/UNKNOWNはボタンなし、UNKNOWNは連絡導線のみ(§3) |
| 4 | 再読込を挟む重複要求 | **採用** — 起票前GETの重複ガード+既存要求リンク(§3共通2、受入7) |
| 5 | RELEASEに停止者・理由表示 | **採用** — CANCEL_REQUESTパース済みの材料を表示(§3) |
| 6 | 403の専用分岐 | **採用**(§3共通3、受入8) |
| 7 | 成功文言の開始/完了混同 | **採用** — 文言分離(§3共通1) |
| 追 | 受入の穴3点 | **採用** — 受入5/7/8へ反映 |

**第2巡(同日)**:

| # | 指摘 | 採否・判断 |
| --- | --- | --- |
| 1 | 終端Runを閉じる手段が製品に不存在(ARCHIVED書込経路なし — コード確認済み) | **P2-10へ正式切り出し**(CLOSE要求→`archive-run` CLI→ポーラー。人が書くのは要求アプリのみ・実行管理を書くのは機械、の規律を維持。状態を書く新CLIのためP2-10で個別レビュー)。暫定の時限フィルタは**不採用**(未対応が黙って消える方が危険)。§1但し書きへ明記 |
| 2 | 抽出条件とポーラー受理条件のズレ(`resume_allowed`欠落) | **採用** — ボタン表示条件を受理条件と同じ2条件へ(§2-3)。将来の「必ず拒否されるボタン」を先回り防止 |
| 3 | 重複ガードGET失敗時の挙動未定義 | **採用** — fail-open+注記表示。GET 403はPOST 403の文言に流さない(§3共通2/3) |
| 4 | 「挙動不変」の自己矛盾と本番表示の無告知変化 | **採用** — 文言を起票機能に限定し、M4へ周知項目を追加(§4) |
| 5 | STOP後のセクション移動の説明不足 | **採用** — ops-first-responseへ1行(§5) |

**第3巡(同日)**:

| # | 指摘 | 採否・判断 |
| --- | --- | --- |
| 1 | 起票済み行が押す前に見分けられない | **採用** — 処理待ちバッジ(要求へのリンク付き、ボタン置換)。重複ガードの完成形として§3共通4へ。GET失敗はfail-open |
| 2a | 2条件のボタン抑止に受入がない(実機再現不能) | **採用** — 受入13へ「単体のみで担保」と明記。M0チェック項目「仕様条件追加=受入同時追加」も追加 |
| 2b | ガードGET fail-openに受入がない | **採用** — 受入14/15 |
| 3 | ボタンなし理由の表示が行によって不統一 | **採用** — 2条件で弾かれる行へ「再開が無効化されています」(§2-3) |
| 4 | UNKNOWN連絡導線の実体が未定義 | **採用** — 固定文言+Run IDワンクリックコピー(§3表・受入16) |
| 5 | 「他N件」が未対応件数と誤読される | **採用** — ラベルを「他N件(決着済みを含む)」へ(§2-2) |
| 6 | §7 M0の版数表記 | **採用** — 「最新版」参照へ(版数ドリフト防止) |

**M0 Codexレビュー(同日、[実装計画](./p2-09-implementation-plan.md)§2 — 17所見)**: FLAWED 9件・NEEDS-CLARIFICATION 3件を全採用し、実装計画のM0補正を補足契約化(冒頭参照)。主な確定: 全30セル判定表(不整合セルはINVALID=fail-closed)、複数pending要求の表示(隠さず最古#id+件数)、pendingはボタン置換ではなく行の最優先運用状態(UNKNOWNの連絡案内と併記)、要求POST bodyの固定+作成後GETでparseRequestRecord合格を受入化、セクション別の失敗分離。
